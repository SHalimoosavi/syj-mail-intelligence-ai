# Server-side only — never exposed to the browser (no NEXT_PUBLIC_ prefix).
# Read by app/api/backend/[...path]/route.ts, which proxies the dashboard's
# requests to FastAPI and attaches the key there.
BACKEND_API_URL=http://localhost:8000
BACKEND_API_KEY=
